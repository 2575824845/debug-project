<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoming.lifemanager.mapper.UserMapper">
    <select id="login" resultType="com.xiaoming.lifemanager.entity.User">
        SELECT id, username, password, nickname, avatar
        FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <update id="updatePassword">
        UPDATE user
        SET password = #{newPassword}
        WHERE id = #{id}
    </update>

    <select id="selectById" resultType="com.xiaoming.lifemanager.entity.User">
        SELECT id, username, nickname, avatar
        FROM user
        WHERE id = #{id}
    </select>
    <update id="updateAvatar">
        UPDATE user
        SET avatar = #{avatar}
        WHERE id = #{id}
    </update>
</mapper>