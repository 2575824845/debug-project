<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaoming.lifemanager.mapper.ScheduleMapper">
    <insert id="addSchedule" parameterType="com.xiaoming.lifemanager.entity.Schedule">
        INSERT INTO schedule (user_id, title, content, start_time, end_time, tag, remind)
        VALUES (#{userId}, #{title}, #{content}, #{startTime}, #{endTime}, #{tag}, #{remind})
    </insert>

    <select id="selectAllByUserId" resultType="com.xiaoming.lifemanager.entity.Schedule">
        SELECT id, user_id, title, content, start_time, end_time, tag, remind
        FROM schedule
        WHERE user_id = #{userId}
        ORDER BY start_time DESC
    </select>

    <update id="updateSchedule" parameterType="com.xiaoming.lifemanager.entity.Schedule">
        UPDATE schedule
        SET title = #{title}, content = #{content}, start_time = #{startTime},
            end_time = #{endTime}, tag = #{tag}, remind = #{remind}
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteSchedule">
        DELETE FROM schedule WHERE id = #{id}
    </delete>
</mapper>